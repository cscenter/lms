---
- name: install pip
  easy_install: name=pip

- name: install global Python stuff
  pip:
    name: "{{ item.name }}"
    version: "{{ item.version }}"
  with_items:
    - name: ipython
      version: "4.0.0"
    - name: virtualenv
      version: "13.1.1"
    - name: uwsgi
      version: "2.0.11.1"
    - name: uwsgitop
      version: "0.8"
    - name: psycopg2
      version: "2.6.1"